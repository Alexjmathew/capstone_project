<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="min-h-full bg-white">

    <!-- Header Section with Red Accents -->
    <header class="bg-white border-b-2 border-red-100 shadow-sm">
        <div class="max-w-6xl mx-auto px-4 py-3">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-3">
                    <div class="bg-red-500 p-2 rounded-lg">
                        <i class="fas fa-tachometer-alt text-white"></i>
                    </div>
                    <h1 class="text-xl font-bold text-gray-800">Dashboard</h1>
                </div>
                
                <nav class="flex items-center space-x-5">
                    <% if (userRole === 'educator') { %>
                        <a href="/educator" class="text-gray-600 hover:text-red-600 font-medium transition-colors">
                            <i class="fas fa-chalkboard-teacher mr-1"></i>
                            Educator Home
                        </a>
                    <% } else { %>
                        <a href="/student" class="text-gray-600 hover:text-red-600 font-medium transition-colors">
                            <i class="fas fa-user-graduate mr-1"></i>
                            Student Home
                        </a>
                    <% } %>
                    
                    <a href="/password" class="text-gray-600 hover:text-red-600 font-medium transition-colors">
                        <i class="fas fa-key mr-1"></i>
                        Update Password
                    </a>
                    
                    <div class="flex items-center space-x-4">
                        <a href="/" class="text-gray-600 hover:text-red-600 font-medium transition-colors">
                            <i class="fas fa-sign-in-alt mr-1"></i>
                            Login
                        </a>
                        
                        <form action="/signout" method="get">
                            <button class="bg-red-600 hover:bg-red-700 text-white py-2 px-4 rounded-lg font-medium transition-colors flex items-center">
                                <i class="fas fa-sign-out-alt mr-2"></i>
                                Sign Out
                            </button>
                        </form>
                    </div>
                </nav>
            </div>
        </div>
    </header>

    <!-- Quick Stats Banner -->
    <div class="bg-red-50 border-b border-red-100">
        <div class="max-w-6xl mx-auto px-4 py-3">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-2 text-red-800">
                    <i class="fas fa-info-circle"></i>
                    <span class="text-sm font-medium">Welcome back! You have <% if (userRole === 'educator') { %>3 new submissions<% } else { %>2 pending assignments<% } %></span>
                </div>
                <span class="text-xs text-red-600"><%= new Date().toLocaleDateString() %></span>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <main class="max-w-6xl mx-auto px-4 py-6">
        <div class="bg-white border border-gray-200 rounded-lg shadow-sm p-6">
            <h2 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
                <i class="fas fa-<%= userRole === 'educator' ? 'chalkboard' : 'book' %> mr-2 text-red-500"></i>
                Quick Access
            </h2>
            
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
                <% if (userRole === 'educator') { %>
                    <a href="#" class="p-4 border border-gray-200 rounded-lg hover:border-red-200 hover:bg-red-50 transition-colors">
                        <div class="flex items-center space-x-3">
                            <div class="bg-red-100 p-2 rounded-full">
                                <i class="fas fa-plus-circle text-red-600"></i>
                            </div>
                            <span class="font-medium text-gray-700">New Course</span>
                        </div>
                    </a>
                    <a href="#" class="p-4 border border-gray-200 rounded-lg hover:border-red-200 hover:bg-red-50 transition-colors">
                        <div class="flex items-center space-x-3">
                            <div class="bg-red-100 p-2 rounded-full">
                                <i class="fas fa-tasks text-red-600"></i>
                            </div>
                            <span class="font-medium text-gray-700">Grade Work</span>
                        </div>
                    </a>
                <% } else { %>
                    <a href="#" class="p-4 border border-gray-200 rounded-lg hover:border-red-200 hover:bg-red-50 transition-colors">
                        <div class="flex items-center space-x-3">
                            <div class="bg-red-100 p-2 rounded-full">
                                <i class="fas fa-book-open text-red-600"></i>
                            </div>
                            <span class="font-medium text-gray-700">My Courses</span>
                        </div>
                    </a>
                    <a href="#" class="p-4 border border-gray-200 rounded-lg hover:border-red-200 hover:bg-red-50 transition-colors">
                        <div class="flex items-center space-x-3">
                            <div class="bg-red-100 p-2 rounded-full">
                                <i class="fas fa-file-upload text-red-600"></i>
                            </div>
                            <span class="font-medium text-gray-700">Submit Work</span>
                        </div>
                    </a>
                <% } %>
                
                <a href="/password" class="p-4 border border-gray-200 rounded-lg hover:border-red-200 hover:bg-red-50 transition-colors">
                    <div class="flex items-center space-x-3">
                        <div class="bg-red-100 p-2 rounded-full">
                            <i class="fas fa-key text-red-600"></i>
                        </div>
                        <span class="font-medium text-gray-700">Password Settings</span>
                    </div>
                </a>
            </div>
        </div>
    </main>

</body>
</html>
